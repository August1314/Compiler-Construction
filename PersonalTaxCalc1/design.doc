个人所得税计算器 - 面向对象程序设计文档
==========================================

一、系统概述
------------
本系统是一个基于命令行界面的个人所得税计算器应用程序，用于根据用户输入的当月工资薪金总额计算应缴纳的个人所得税额。系统设计遵循面向对象编程原则，支持起征点和税率的动态调整，以适应未来个人所得税法的可能变化。

二、需求分析
------------
2.1 功能需求
   (1) 基本功能：根据工资薪金总额计算个人所得税
   (2) 扩展功能 1：修改个人所得税起征点
   (3) 扩展功能 2：修改各级税率
   (4) 用户界面：命令行菜单交互

2.2 非功能需求
   (1) 可维护性：代码结构清晰，易于理解和修改
   (2) 可扩展性：能够适应税法政策的变化
   (3) 健壮性：具备异常处理能力，防止非法输入导致程序崩溃
   (4) 可复用性：类的设计具有独立性和通用性

三、系统设计
------------
3.1 架构设计
   系统采用分层架构设计：
   - 表示层：负责用户界面和交互（main 方法、菜单显示）
   - 业务逻辑层：负责个税计算核心逻辑（calculateTax 方法）
   - 数据层：负责数据存储和管理（TaxRate 类、税率数组）

3.2 类设计

3.2.1 PersonalTaxCalculator 类（主类）
   职责：作为程序的入口和控制器，协调各个功能的执行
   
   属性：
   - taxThreshold: double 类型，静态私有属性
     说明：个人所得税起征点，默认为 5000 元
     设计理由：使用静态属性便于全局访问，私有封装保证数据安全
   
   - taxRates: TaxRate[] 类型，静态私有属性
     说明：税率表数组，存储 7 级税率信息
     设计理由：将税率表集中管理，便于查询和修改
   
   方法：
   - main(String[] args): 静态公有方法
     说明：程序入口，实现主循环和菜单调度
     设计理由：作为应用程序的标准入口点
   
   - displayMenu(): 静态私有方法
     说明：显示功能菜单和当前状态
     设计理由：单一职责原则，将显示逻辑独立封装
   
   - calculateTax(Scanner): 静态私有方法
     说明：执行个税计算的核心业务逻辑
     设计理由：将计算功能独立封装，符合高内聚原则
   
   - adjustThreshold(Scanner): 静态私有方法
     说明：提供修改起征点的功能
     设计理由：将配置修改功能独立封装
   
   - adjustTaxRates(Scanner): 静态私有方法
     说明：提供修改税率的功能
     设计理由：将税率配置功能独立封装

3.2.2 TaxRate 类（辅助类）
   职责：封装税率等级信息，提供数据访问接口
   
   属性：
   - start: double 类型，私有属性
     说明：该级距的起始金额
   
   - end: double 类型，私有属性
     说明：该级距的结束金额（最高级为 Double.MAX_VALUE）
   
   - rate: double 类型，私有属性
     说明：该级距适用的税率（0-1 之间的小数）
   
   设计理由：
   - 封装税率相关数据，符合数据抽象原则
   - 提供 getter/setter 方法，实现信息隐藏
   - 独立的税率类使数据结构更清晰，便于扩展
   
   方法：
   - TaxRate(double, double, double): 公有构造方法
     说明：初始化税率对象的三个属性
   
   - getStart(): double 类型，公有方法
     说明：获取起始金额
   
   - getEnd(): double 类型，公有方法
     说明：获取结束金额
   
   - getRate(): double 类型，公有方法
     说明：获取税率值
   
   - setRate(double): void 类型，公有方法
     说明：设置新的税率值
     设计理由：仅提供税率的可变接口，保持区间不可变

四、关键算法设计
--------------
4.1 个人所得税计算算法
   采用超额累进税率计算方法：
   
   步骤：
   (1) 计算应纳税所得额 = 工资总额 - 起征点
   (2) 如果应纳税所得额 <= 0，无需纳税
   (3) 否则，遍历税率表，对每个级距：
       - 计算该级距内的应税金额
       - 累加该级距的税额
   (4) 返回累计税额
   
   伪代码：
   taxableIncome = income - taxThreshold
   if taxableIncome <= 0:
       return 0
   
   tax = 0
   for each rate in taxRates:
       if taxableIncome > rate.start:
           amount = min(taxableIncome, rate.end) - rate.start
           tax += amount * rate.rate
   
   return tax

4.2 异常处理机制
   系统采用 try-catch 机制处理可能的异常情况：
   
   - InputMismatchException：捕获用户输入类型错误
   - 边界检查：验证输入值的合法性（如非负、范围等）
   - 友好的错误提示：向用户提供清晰的错误信息

五、面向对象特性应用
--------------------
5.1 封装（Encapsulation）
   - TaxRate 类的所有属性均为 private，通过公共方法访问
   - PersonalTaxCalculator 的配置数据对外隐藏
   - 内部实现细节不暴露给外部

5.2 数据抽象（Data Abstraction）
   - TaxRate 类抽象了"税率等级"这一概念
   - 将复杂的税率表简化为对象数组
   - 用户只需关注接口，无需了解内部实现

5.3 信息隐藏（Information Hiding）
   - 类的内部状态不被外部直接访问
   - 通过受控的接口修改数据，保证数据一致性
   - 实现细节的变化不影响外部调用

5.4 模块化（Modularity）
   - 系统划分为 PersonalTaxCalculator 和 TaxRate 两个类
   - 每个方法职责单一，功能明确
   - 低耦合设计，便于独立测试和维护

六、可扩展性设计
----------------
6.1 起征点调整
   - 起征点存储在静态变量中，可随时修改
   - 无需重新编译即可适应政策变化

6.2 税率调整
   - 税率表以对象数组形式存储
   - 支持运行时修改各级税率
   - 如需增加税率级数，只需扩展 taxRates 数组

6.3 潜在改进方向
   (1) 可将税率表配置外置到配置文件（如 XML、JSON）
   (2) 可增加图形用户界面（GUI）
   (3) 可支持其他类型的所得计税（如劳务报酬、稿酬等）
   (4) 可增加历史记录和统计功能

七、测试策略
------------
7.1 单元测试
   - 测试正常收入水平的计税准确性
   - 测试边界情况（起征点、各级距边界）
   - 测试异常输入的处理

7.2 回归测试
   - 提供多个测试用例脚本
   - 每次修改后运行所有测试确保正确性
   - 记录预期输出与实际输出对比

7.3 测试用例设计
   用例 1：中等收入（50,000 元）- 验证标准计算逻辑
   用例 2：低收入（3,000 元）- 验证未达起征点情况
   用例 3：高收入（100,000 元）- 验证最高税率级距
   用例 4：边界值（5,000 元）- 验证起征点边界

八、编码规范遵循
----------------
8.1 命名规范
   - 类名：大驼峰式（PersonalTaxCalculator, TaxRate）
   - 方法名：小驼峰式（calculateTax, displayMenu）
   - 变量名：有意义的英文单词（taxThreshold, taxableIncome）
   - 常量：全大写（本程序无显式常量定义）

8.2 注释规范
   - 类注释：说明类的用途和功能
   - 方法注释：使用 Javadoc 格式描述方法功能
   - 行内注释：解释关键代码逻辑

8.3 版面规范
   - 缩进：4 个空格
   - 空行：方法间、逻辑段落间使用空行分隔
   - 行长：控制在合理范围内

九、总结
--------
本系统采用面向对象的设计方法，实现了个人所得税计算的基本功能。
通过合理的类设计和方法划分，达到了高内聚、低耦合的设计目标。
系统具备良好的可读性、可维护性和可扩展性，符合 Java 编码规范要求。

设计者：[请填写姓名]
日  期：2025 年 3 月