# Quiz 1

1、请给出下面描述的正则表达式：
   (1) 以1开头10结尾的二进制数串；（10分）

2、考虑定义在字母表 Σ = {a, b} 上的正则表达式（a | ab）*：
   (1) 画出识别该语言的不确定有限自动机（NFA）；（20分）
   (2) 将得到的NFA转化为等价的DFA，给出转换表和状态转换图；（40分）
   (3) 上一问得到的DFA是否为最小化的DFA，若为否则给出最小化DFA。（30分）

---

# Quiz 2

## 一、对于下列产生式定义的文法 G：

```
S → Sb A | a
B → Sb
A → Bc | d
```

(1) 改造文法 G 为 G'，以消除 G 中的左递归。（20分）  
(2) 表1中给出了文法 G' 每一个非终结符号的 FOLLOW 集，请补齐对应的 FIRST 集合。（40分）  
(3) 请补齐表2中文法 G' 的 LL(1) 分析表，并解释 G' 是否为 LL(1) 文法。（40分）

表1 FIRST 集合、FOLLOW 集合

| 非终结符 | FIRST | FOLLOW |
|---|---|---|
| S | | b, $ |
| S' | | b, $ |
| B | | c |
| A | | b, $ |

表2 LL(1) 分析表

| 非终结符 | a | b | d | $ |
|---|---|---|---|---|
| S | S → a S' | | | |
| S' | | | (2) | |
| B | | (1) | | |
| A | A → Bc | | A → d | |

---

# Quiz 3

## 一、分析题（100分）

对于文法 G3(S)：

```
S → A
A → BA | ε
B → aB | b
```

将文法 G3(S) 增广为 G3(S') 后，构建识别该文法所产生的活前缀的 DFA 如右图所示。其中，I0 为起始状态，所有状态均为终止状态。

1. 请求出 A、B 的 first 集（20分）  
2. 请将上述 DFA 中的 1~10 补充完（80分）

DFA 图中已给出的项目集（部分）如下，原图文字可能有 OCR 误差，请以原图为准：

- I1: S' → S·, ε
- I2: A → B·A, ε
- I3: B → b·a, b / ε
- I4: S → A·, ε
- 其余状态 ④~⑩ 待补充

---

# Quiz 4

1. 右表定义了二进制整数常量的语法规则。

(1) 补充语义规则以求二进制整数常量的十进制值，并存放在开始符号的综合属性 value 中。  
例如，输入串 101，S.value 应该是 5。

| 语法规则 | 语义规则 |
|---|---|
| S → S₁ L | S.value = ... |
| S → L | S.value = L.value |
| L → 0 | L.value = ... |
| L → 1 | L.value = ... |

(2) 该语法制导定义中 S.value 是综合属性还是继承属性？

(3) 补全右侧的带标注的抽象语法树。

标注树节点值填空（按原图顺序）：

```
S₁.value = _
S₁.value = _
S₁.value = 1
L.value = 1
L.value = 0
L.value = 1
L.value = 0
L.value = 1
```

---

# Quiz 5

## 100分 对于如下文法G1和G2：

### G1:
```
S → A c B
A → a A b | c
B → d
```

### G2:
```
S → A c B
A → a A b | c
B → d | d B
```

---

1. **[10分]** 求文法G1的增广文法(Augmented Grammar) G3，并为G3中的产生式编号（从0开始）。  
2. **[25分]** 对文法G3构建LR(0)解析的有穷自动机(FA)，包括状态和转换。  
   （提示：项目集闭包(closure of item sets)）  
3. **[30分]** 按下表格式构建文法G3的LR(0)分析表，如有需要，请自行添加更多行。

| State | a | b | c | d | $ | S | A | B |
|---|---|---|---|---|---|---|---|---|
| 0 | | | | | | | | |
| I | | | | | | | | |
| ... | | | | | | | | |

> 说明：ACTION 列对应 a、b、c、d、$；GOTO 列对应 S、A、B。

4. **[35分]** 列出解析输入串 acbcd 的完整分析过程，包括每一步输入串和解析栈变化及采取的具体动作（如果是reduce请指明归约所用的产生式），直到最后执行Accept或Error动作。

| Stack（左侧为栈底） | Input（右侧为底） | Action | GOTO |
|---|---|---|---|
| | | | |